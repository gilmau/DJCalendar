<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_margin="30dp"
    android:paddingBottom="30dp">
    <ImageView
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:src="@drawable/reset_password"
        />
    <TextView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:text="Forgot  \n Your Password ?"
        android:gravity="center"
        android:textColor="@color/blackTextColor"
        android:textStyle="bold"/>


    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingBottom="20dp"
        android:layout_marginHorizontal="40dp"
        android:layout_marginTop="20dp"
        android:background="@color/lists_background">

            <TextView
                android:id="@+id/email_rest_password"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:hint="@string/email_const"
                android:inputType="textEmailAddress" />

        <Button
            style="@style/Dialog_Buttons"
            android:id="@+id/sign_in_btn_logdia"
            android:text="We send the password to your Email  "
            android:textSize="10dp"
            android:background="@color/colorPrimary"
            />
    </LinearLayout>
</LinearLayout>